{
    "type": "googlecompute",
    "account_file": "{{user `google_account_file`}}",
    "project_id": "{{user `google_project_id`}}",
    "source_image": "{{user `google_source_image`}}",
    "zone": "{{user `google_zone`}}",
    "image_name": "coreos-{{user `coreos_realese`}}-{{user `coreos_version`}}-{{timestamp}}",
    "image_description": "{{user `google_image_description`}}",
    "ssh_username": "core",
    "machine_type": "n1-standard-1",
    "disk_size": 33
}
